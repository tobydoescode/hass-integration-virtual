services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:2026.7.4
    ports:
      - "8123:8123"
    volumes:
      - ha-config:/config
      - ./custom_components/virtual:/config/custom_components/virtual:ro
    restart: unless-stopped

volumes:
  ha-config:
